<%= bootstrap_form_for @outing do |f| %>
  <%= f.date_field :day, label: 'Giorno:' %>
  <%= f.collection_select :location_id, @locations, :id, :name, include_blank: true, label: 'Luogo' %>
  <%= f.time_field :time, label: 'Ora:' %>
  <%= f.submit 'Aggiorna' %>
<% end %>